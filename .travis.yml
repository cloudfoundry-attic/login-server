language: java
before_install: 
  - mvn -v
  - git pull --unshallow
install: 
  -  mvn -U clean --quiet -DskipTests=true
  -  mvn -U install --quiet -DskipTests=true
script: 
  - ./.run-script.sh
  - mvn test
  - kill -9 `cat mvn.pid`
  - rm -f mvn.pid
